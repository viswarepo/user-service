spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/sms_dbs
    username: sms
    password: password
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        #ddl-auto: update
        #show-sql: true
        properties:
          hibernate:
            format_sql:true
          dialect: org.hibernate.dialect.PostgreSQLDialect
          open-in-view : false
